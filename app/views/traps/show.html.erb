<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @trap.id %>
</p>
<br>
<br>
<%= link_to 'Edit', edit_trap_path(@trap) %> |
<%= link_to 'Back', traps_path %>
<table>
	<tr>
		<%= render @trap.requests %>
	</tr>
</table>
<%= render 'requests/form' %>

